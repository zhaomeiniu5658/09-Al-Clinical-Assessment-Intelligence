# AI 临床量表智能质控系统项目交接总说明

> 文档版本：2026-09-25  
> 当前分支：`main`  
> 项目目录：`/Users/zhaoy/Documents/project/09-Al-Clinical-Assessment-Intelligence`  
> 用途：帮助新接手人员快速理解项目、启动项目、定位代码和继续开发。

## 1. 项目一句话说明

这是一个“AI 临床量表智能质控系统”，用户上传医患对话音频和医生填写的量表评分 Excel，系统先通过讯飞 ASR 将音频转录为文本，再调用 Dify Workflow 进行量表质控分析，最后在前端展示医生评分、AI 评分、AI 评分依据、差异和逐项目人工复核结果。

项目同时包含一套“夸克智汇平台”入口静态界面，用于展示平台首页、平台导航和 CNS 商机挖掘页面。

## 2. 当前交接快照

### 2.1 代码和工作区状态

- 当前分支是 `main`。
- 最近一次提交为 `6342803`，提交说明为“提交”。
- 当前工作区有未提交修改，涉及 14 个文件，主要集中在任务处理、Dify 结果解析、医生评分表、逐项目复核和前端页面。
- 接手时不要直接执行 `git reset --hard`、`git checkout --` 或清理未提交文件，先确认这些修改是否需要保留。
- 项目根目录的 `.env` 含有管理员密码、Dify API Key、讯飞密钥等敏感信息，不要提交到 Git，也不要复制到交接文档。

### 2.2 本次检查时的运行状态

本次交接文档生成时，Docker 容器没有运行，`localhost:8000` 和 `localhost:8080` 暂时无法访问。启动命令见第 10 节。

正常启动后，预期服务如下：

| 服务 | 地址 | 用途 |
|---|---|---|
| 前端 | `http://localhost:8080` | 临床量表前端和平台入口 |
| 后端 | `http://localhost:8000` | FastAPI 接口 |
| 后端健康检查 | `http://localhost:8000/health` | 检查后端是否正常 |
| 后端接口文档 | `http://localhost:8000/docs` | Swagger / OpenAPI |
| 临床系统 MySQL | `localhost:3306` | 业务数据库 |
| 本地 Dify | `http://localhost:8081` | Dify 管理台，需单独启动 |

实际端口以项目根目录 `.env` 为准。当前 `.env` 中临床系统端口为：

```text
FRONTEND_PORT=8080
BACKEND_PORT=8000
MYSQL_PORT=3306
```

## 3. 已完成的功能

### 3.1 夸克智汇平台静态入口

已完成：

- 平台登录静态页。
- 夸克智汇平台首页。
- 顶部平台导航栏。
- 平台应用卡片和 Liquid Glass 风格 SVG 图标。
- CNS 商机挖掘静态页面。
- 平台首页的 Dify 入口。
- 平台首页文案、图标、布局和登录卡片位置调整。

当前可用路由：

- `/platform/login`
- `/platform`
- `/platform/research`

注意：平台登录页目前是静态演示登录。页面提交后直接提示登录成功并跳转平台首页，尚未调用后端真实登录接口。

### 3.2 临床量表质控业务

已完成：

- 单用户登录和管理员用户管理后端。
- 新建质控任务。
- 默认量表类型为 `HAMD`。
- 上传医患对话音频。
- 上传医生评分 Excel，支持 `.xls` 和 `.xlsx`。
- 上传后异步执行 ASR 和 AI 质控。
- 任务列表自动刷新任务状态。
- 失败任务重新尝试。
- 查看 ASR 转录文本。
- 查看 AI 质控分析。
- 删除任务，同时清理任务关联的结果、复核记录和上传文件。
- 任务详情页内嵌展示量表项目明细。
- 每一行独立人工复核。
- 每行可保存“复核得分”和“复核意见”。
- 表格显示“差异”“复核得分”“复核意见”“状态”“操作”。
- 复核后状态显示“已复核”，操作列始终显示“复核”。

当前任务详情表格列顺序为：

```text
HAM-D17项目
评分员打分
AI打分
AI打分依据
差异
复核得分
复核意见
状态
操作
```

### 3.3 ASR 与 Dify

已完成：

- 讯飞录音文件转写接口封装。
- 保存原始音频和 ASR 转录文本。
- Dify Workflow 文件上传。
- 将转录文本和医生评分表同时传给 Dify Workflow。
- 解析 Dify 返回的 JSON。
- 兼容 Dify 返回的项目编号、项目名称、`item`、`name` 等不同字段。
- 兼容部分文本表格格式的输出。
- 将医生评分表、AI 评分、评分依据和差异合并成逐项目结果。

当前远程测试 Dify 地址：

```text
http://101.200.145.196:8888
```

Workflow 管理页面：

[http://101.200.145.196:8888/app/4d22d983-a041-4bf9-a403-20de39860ec5/workflow](http://101.200.145.196:8888/app/4d22d983-a041-4bf9-a403-20de39860ec5/workflow)

远程 Workflow 当前约束：

- 只支持 `HAMD` / `HAM-D17`。
- 医生评分 Excel 必须上传。
- Workflow 输入变量默认是 `dialog` 和 `doctor_test`。
- 超时时间当前为 300 秒。

## 4. 系统架构和数据流

```text
用户浏览器
    |
    v
Vue 3 + Element Plus 前端
    |
    | /api/v1 反向代理
    v
FastAPI 后端
    |
    +--> MySQL 8.4
    |
    +--> 项目目录 data/storage
    |       +--> audio/          原始音频
    |       +--> doctor_tests/   医生评分表
    |       +--> knowledge/      知识库附件
    |
    +--> 讯飞 ASR
    |
    +--> Dify Workflow
```

一次完整任务的处理过程：

1. 用户在前端选择量表类型。
2. 用户上传音频和医生评分 Excel。
3. 前端调用 `POST /api/v1/tasks` 创建任务。
4. 后端保存文件，解析医生评分表中的 HAM-D17 项目和医生评分。
5. 后端将任务放入 FastAPI `BackgroundTasks`。
6. 后台调用讯飞录音文件转写接口。
7. ASR 完成后保存 `asr_text`。
8. 后台上传医生评分表到 Dify，并将转录文本和文件引用发送到 Workflow。
9. 后端解析 Workflow 输出，并合并医生评分、AI 评分、评分依据和差异。
10. 任务状态改为 `COMPLETED`。
11. 前端列表自动刷新，详情页显示逐项目结果。
12. 用户可对任意一行进行复核，复核内容保存在 `qc_results.item_results` JSON 中。

## 5. 项目文件地图

### 5.1 根目录

```text
.
├── .env                         本机运行配置，含敏感信息，不提交
├── .env.example                 环境变量模板
├── docker-compose.yml            临床系统的 MySQL、后端、前端编排
├── DOCKER部署.md                 Docker 部署、迁移和备份说明
├── README.md                    简版项目说明
├── 项目交接总说明.md             本文，当前最完整的交接入口
├── AI临床量表智能质控系统.yml     可导入 Dify 的 Workflow 文件
├── backend/                     FastAPI 后端
├── frontend/                    Vue 前端
├── data/                        项目运行数据
├── dify/                        独立 Dify Docker 部署
├── docs/                        UI、部署、需求和历史交接资料
├── delivery/                    静态页面交付快照
└── scripts/                    数据迁移等辅助脚本
```

### 5.2 后端目录

```text
backend/
├── Dockerfile
├── requirements.txt
├── alembic.ini
├── alembic/
│   ├── env.py
│   └── versions/
│       ├── 0001_initial.py
│       ├── 0002_xfyun_asr_settings.py
│       ├── 0003_user_management.py
│       ├── 0004_knowledge_base.py
│       ├── 0005_knowledge_categories_external_api.py
│       ├── 0006_qc_item_results.py
│       └── 0007_dify_workflow_doctor_test.py
├── app/
│   ├── main.py                  FastAPI 应用入口和健康检查
│   ├── api/v1/
│   │   ├── router.py            汇总所有 API 路由
│   │   ├── auth.py              登录、当前用户
│   │   ├── tasks.py             任务创建、列表、详情、删除、重试、复核
│   │   ├── dashboard.py         工作台统计
│   │   ├── users.py             用户管理
│   │   ├── settings.py          讯飞配置
│   │   ├── knowledge.py         知识库管理
│   │   └── external_knowledge.py 知识库对外接口
│   ├── core/
│   │   ├── config.py            Pydantic Settings 和环境变量
│   │   └── security.py          JWT、密码哈希和鉴权
│   ├── db/
│   │   ├── session.py           SQLAlchemy 数据库连接
│   │   ├── base.py              模型基类导入
│   │   └── init_db.py           启动时创建或补齐管理员
│   ├── models/
│   │   ├── assessment.py        任务、质控结果、审核记录
│   │   ├── user.py              用户
│   │   ├── knowledge.py         知识库
│   │   └── system.py            系统设置
│   ├── schemas/                 Pydantic 请求和响应模型
│   ├── services/
│   │   ├── ai_qc.py             Dify Workflow / OpenAI 兼容调用
│   │   ├── doctor_test.py       医生评分 Excel 解析
│   │   ├── qc_items.py          AI 和医生项目结果合并
│   │   └── xfyun_asr.py         讯飞 ASR
│   └── tasks/worker.py          后台任务处理主流程
└── tests/
    ├── test_ai_qc.py            Dify 输出解析和项目合并测试
    ├── test_contracts.py        枚举和接口契约测试
    └── test_xfyun_asr.py        讯飞 ASR 测试
```

### 5.3 前端目录

```text
frontend/
├── Dockerfile
├── package.json
├── package-lock.json
├── vite.config.ts
├── nginx/default.conf           生产环境 Nginx 和后端代理
├── public/platform/             平台静态图片和 SVG 图标
└── src/
    ├── main.ts                  Vue、Pinia、Element Plus 入口
    ├── App.vue                  平台顶部导航和 RouterView
    ├── router/index.ts          页面路由和路由守卫
    ├── api/
    │   ├── client.ts            Axios、Token 保存和 401 处理
    │   ├── auth.ts              登录 API
    │   ├── tasks.ts             任务 API
    │   ├── dashboard.ts         工作台 API
    │   ├── users.ts             用户 API
    │   ├── settings.ts          系统设置 API
    │   └── knowledge.ts         知识库 API
    ├── stores/auth.ts            登录用户和 Token 状态
    ├── components/
    │   ├── AppShell.vue          临床系统左侧菜单和顶部工作区
    │   └── PlatformTopbar.vue    夸克智汇平台顶部导航
    ├── views/
    │   ├── PlatformLoginView.vue 平台登录静态页
    │   ├── PlatformHomeView.vue  平台首页
    │   ├── PlatformResearchView.vue CNS 商机挖掘
    │   ├── DashboardView.vue     临床系统工作台
    │   ├── QcListView.vue        质控任务列表和上传弹窗
    │   ├── TaskDetailView.vue    任务详情、逐项目复核
    │   ├── UsersView.vue         管理员用户管理
    │   ├── SettingsView.vue      讯飞 ASR 设置
    │   └── KnowledgeView.vue     知识库页面代码，当前未挂载到主路由
    ├── types/                    前端 TypeScript 类型
    └── styles/app.css            全局样式
```

### 5.4 静态页面交付目录

```text
delivery/quarkmed-platform-pages/
├── src/views/
│   ├── PlatformLoginView.vue
│   └── PlatformHomeView.vue
└── public/platform/
    ├── login-left.png
    ├── home-hero.png
    └── icons/
        ├── ai.svg
        ├── bi.svg
        ├── ops.svg
        ├── bd.svg
        ├── research.svg
        ├── sctms.svg
        ├── pv.svg
        └── scale.svg
```

`delivery/` 是静态页面交付快照。实际运行项目使用的是 `frontend/` 下的页面和资源。

## 6. 前端路由和页面关系

| 路由 | 页面 | 当前说明 |
|---|---|---|
| `/platform/login` | `PlatformLoginView.vue` | 夸克智汇平台静态登录 |
| `/platform` | `PlatformHomeView.vue` | 平台首页 |
| `/platform/research` | `PlatformResearchView.vue` | CNS 商机挖掘静态页 |
| `/login` | `PlatformLoginView.vue` | 临床系统未登录时也会进入该页面 |
| `/` | `QcListView.vue` | 质控任务列表，需要真实 Token |
| `/tasks/:id` | `TaskDetailView.vue` | 任务详情和逐项目复核 |
| `/dashboard` | `DashboardView.vue` | 临床系统工作台 |
| `/settings` | `SettingsView.vue` | 讯飞 ASR 配置 |
| `/users` | `UsersView.vue` | 管理员用户管理 |

顶部平台导航由 `frontend/src/components/PlatformTopbar.vue` 控制。部分导航项仍是静态提示“功能正在建设中”，没有真实业务页面。

## 7. 后端 API 清单

后端统一前缀：

```text
/api/v1
```

### 7.1 健康检查和认证

```text
GET  /health
POST /api/v1/auth/login
GET  /api/v1/auth/me
```

### 7.2 质控任务

```text
POST   /api/v1/tasks
GET    /api/v1/tasks
GET    /api/v1/tasks/{task_id}
GET    /api/v1/tasks/{task_id}/audio
DELETE /api/v1/tasks/{task_id}
POST   /api/v1/tasks/{task_id}/retry
POST   /api/v1/tasks/{task_id}/review
POST   /api/v1/tasks/{task_id}/items/{item_index}/review
```

创建任务使用 `multipart/form-data`：

```text
scale_type=HAMD
audio_file=<音频文件>
doctor_test_file=<.xls 或 .xlsx 文件>
```

逐项目复核 JSON：

```json
{
  "review_score": 2,
  "review_opinion": "根据原始对话重新确认评分依据"
}
```

### 7.3 工作台、用户和设置

```text
GET    /api/v1/dashboard/overview

GET    /api/v1/users
POST   /api/v1/users
PUT    /api/v1/users/{user_id}
PATCH  /api/v1/users/{user_id}/status
DELETE /api/v1/users/{user_id}

GET    /api/v1/settings/xfyun-asr
PUT    /api/v1/settings/xfyun-asr
```

### 7.4 知识库

```text
GET    /api/v1/knowledge
POST   /api/v1/knowledge
GET    /api/v1/knowledge/{entry_id}
PUT    /api/v1/knowledge/{entry_id}
DELETE /api/v1/knowledge/{entry_id}
GET    /api/v1/knowledge/{entry_id}/attachment

GET    /api/v1/knowledge/categories
POST   /api/v1/knowledge/categories
DELETE /api/v1/knowledge/categories/{category_id}

GET    /api/v1/knowledge/external-api
PUT    /api/v1/knowledge/external-api

GET    /api/v1/external/knowledge
GET    /api/v1/external/knowledge/{entry_id}
GET    /api/v1/external/knowledge/{entry_id}/attachment
GET    /api/v1/external/knowledge/categories
```

## 8. Dify 接入说明

### 8.1 当前项目配置

当前根目录 `.env` 的关键非敏感配置为：

```env
DIFY_PROTOCOL=workflow
DIFY_WORKFLOW_BASE_URL=http://101.200.145.196:8888
DIFY_WORKFLOW_USER=clinical-qc
DIFY_WORKFLOW_DIALOG_INPUT=dialog
DIFY_WORKFLOW_DOCTOR_TEST_INPUT=doctor_test
DIFY_WORKFLOW_REQUIRE_DOCTOR_TEST_FILE=true
DIFY_TIMEOUT_SECONDS=300
```

API Key 只存在 `.env` 的 `DIFY_WORKFLOW_API_KEY`，交接时不要把真实值写进文档、提交记录或截图。

### 8.2 后端调用流程

1. 调用 `POST {DIFY_WORKFLOW_BASE_URL}/v1/files/upload` 上传医生评分 Excel。
2. 得到 Dify `upload_file_id`。
3. 调用 `POST {DIFY_WORKFLOW_BASE_URL}/v1/workflows/run`。
4. 请求头使用：

```text
Authorization: Bearer <DIFY_WORKFLOW_API_KEY>
Content-Type: application/json
```

5. 请求体核心结构：

```json
{
  "inputs": {
    "dialog": "ASR 转录文本",
    "doctor_test": {
      "type": "document",
      "transfer_method": "local_file",
      "upload_file_id": "Dify 上传返回的文件 ID"
    }
  },
  "response_mode": "blocking",
  "user": "clinical-qc"
}
```

6. 后端从 `data.outputs` 中读取 `text`、`result`、`output` 或 `answer`。
7. 输出可以是 JSON，也兼容部分 Markdown / 文本表格。
8. 后端将结果标准化为 `QcItemResult`：

```text
hamd_item
doctor_score
ai_score
ai_scoring_basis
difference
review_score
review_opinion
```

### 8.3 Dify Workflow 修改时的注意事项

- 工作流输入变量名必须和 `.env` 中的 `DIFY_WORKFLOW_DIALOG_INPUT`、`DIFY_WORKFLOW_DOCTOR_TEST_INPUT` 保持一致。
- 如果修改输出结构，需要同步检查 `backend/app/services/ai_qc.py` 的 `_extract_workflow_json`、`_normalize_item_results` 和 `_result_from_output`。
- 当前后端限制 Workflow 模式只能提交 `HAMD`。如果要开放 HAMA 或 PHQ-9，需要更新后端限制、医生评分表解析器和 Dify 工作流。
- 如果 Dify 输出只包含差异项目，后端会通过 `merge_item_results` 补全医生评分表中的其他项目。
- Workflow 超时当前为 300 秒，不建议在接口层改成短超时，否则长音频会频繁失败。

## 9. 数据库和持久化文件

### 9.1 MySQL

临床系统使用 Docker 内的 MySQL 8.4，不依赖宿主机本地 MySQL。

```text
数据库名：clinical_qc
容器服务：mysql
容器内端口：3306
项目目录数据：data/mysql/
```

### 9.2 主要数据表

| 表 | 用途 |
|---|---|
| `users` | 用户、管理员、密码哈希、启停状态 |
| `assessment_tasks` | 任务、音频、医生评分表、状态、ASR 文本 |
| `qc_results` | 总分、分析结果、逐项目结果、Dify 原始响应 |
| `review_records` | 旧版任务级审核记录，仍保留兼容 |
| `xfyun_asr_settings` | 讯飞 ASR 设置 |
| 知识库相关表 | 知识内容、分类、外部 API 配置 |

### 9.3 Alembic 迁移

后端容器启动时会执行：

```text
alembic upgrade head
```

最新迁移 `0007_dify_workflow_doctor_test.py` 为任务增加：

```text
doctor_test_path
doctor_test_original_name
doctor_test_mime_type
doctor_test_size
```

不要删除或重命名已经执行过的迁移文件。

### 9.4 文件存储

```text
data/storage/audio/          原始音频
data/storage/doctor_tests/  医生评分 Excel
data/storage/knowledge/      知识库附件
```

这些目录含有业务数据。迁移电脑或备份时，必须同时保留 `.env`、`data/mysql` 和 `data/storage`。

## 10. 启动、停止和查看日志

### 10.1 启动临床量表系统

在项目根目录执行：

```bash
docker compose up -d --build
docker compose ps
```

健康检查：

```bash
curl http://localhost:8000/health
```

预期返回：

```json
{"status":"ok"}
```

打开前端：

```text
http://localhost:8080
```

### 10.2 启动本地 Dify

本地 Dify 是独立 Compose，不会随根目录 Compose 自动启动：

```bash
cd dify
docker compose up -d
docker compose ps
```

本地 Dify 管理台：

```text
http://localhost:8081
```

停止时使用：

```bash
docker compose down
```

不要使用 `docker compose down -v`，否则可能删除 Dify 数据卷。

### 10.3 日常日志

临床系统：

```bash
docker compose logs -f backend
docker compose logs -f frontend
docker compose logs -f mysql
```

Dify：

```bash
cd dify
docker compose logs -f api
docker compose logs -f nginx
```

### 10.4 停止临床系统

```bash
docker compose down
```

这个命令不会删除项目目录下的 `data/mysql` 和 `data/storage`。不要随意执行 `docker compose down -v`。

## 11. 本地开发和验证

### 11.1 前端

```bash
cd frontend
npm install
npm run dev
```

生产构建：

```bash
npm run build
```

构建包含：

```text
vue-tsc --noEmit
vite build
```

### 11.2 后端

```bash
cd backend
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
alembic upgrade head
uvicorn app.main:app --reload
```

Windows PowerShell 激活虚拟环境：

```powershell
.venv\Scripts\activate
```

### 11.3 后端测试

```bash
cd backend
pytest -q
```

重点测试：

- Dify Workflow JSON 输出解析。
- 中文字段和项目编号映射。
- 文本表格输出解析。
- 医生评分表和 AI 评分合并。
- 任务枚举值契约。

## 12. 管理员和登录说明

默认管理员由根目录 `.env` 决定：

```env
ADMIN_USERNAME=...
ADMIN_PASSWORD=...
```

后端启动时，如果数据库中不存在该用户名，会自动创建管理员；如果已经存在，不会因为修改 `.env` 自动覆盖原密码。

真实后端登录接口是：

```text
POST /api/v1/auth/login
```

登录成功后返回 JWT。勾选记住登录时，Token 保存到 `localStorage`；否则保存到 `sessionStorage`。

**当前需要重点确认的登录问题：**

- `frontend/src/stores/auth.ts` 和 `frontend/src/api/auth.ts` 已实现真实登录调用。
- 但当前 `/login` 和 `/platform/login` 使用的是 `PlatformLoginView.vue`。
- `PlatformLoginView.vue` 当前只做静态演示跳转，没有调用 `auth.login()`。
- 因此临床业务真实登录链路目前需要接手人员确认和修复，否则访问需要 Token 的 `/` 页面可能再次跳回登录页。

建议后续明确一种方案：

1. 平台登录页直接接入 `useAuthStore().login()`，登录成功后进入 `/` 或 `/dashboard`。
2. 平台静态登录和临床真实登录拆成两个页面，分别使用不同路由。

## 13. 当前已知问题和后续待办

### 高优先级

1. **确认平台登录和临床真实登录的关系。** 当前静态登录页未调用后端登录接口。
2. **检查并提交当前工作区的 14 个未提交文件。** 这些修改包含核心 Dify、医生 Excel 和逐项目复核逻辑，不能在未确认前丢弃。
3. **用真实音频、真实医生评分表和测试 Workflow 完整跑通一条任务。**
4. **确认远程 Dify API Key 是否仍有效。** 真实值只在 `.env` 中检查，不要写入文档。

### 中优先级

1. 如果要支持 HAMA / PHQ-9，需要增加对应评分表解析、Workflow 和前端校验。
2. 当前任务使用 FastAPI `BackgroundTasks`，适合单机或低并发；生产高并发时应考虑独立任务队列。
3. 当前平台多个应用入口仍是静态提示，需要逐个接入真实页面或外部系统。
4. `KnowledgeView.vue` 和知识库后端接口仍保留，但当前主导航和路由已移除，需要明确是继续保留还是彻底清理。
5. 任务详情中的旧版任务级审核接口仍保留，当前界面主要使用逐项目复核接口，后续可评估是否下线旧接口。

### 低优先级

1. 优化前端大包体积，当前 Vite 构建会提示部分 chunk 超过 500 kB。
2. 增加端到端测试，覆盖登录、上传、任务轮询、复核和删除。
3. 增加任务权限、操作审计和生产环境日志保留策略。
4. 增加音频编码检查或统一使用 ffmpeg 转码，避免讯飞接口因编码格式拒绝文件。

## 14. 交接时建议按这个顺序操作

### 第一步：保护现有数据和配置

```bash
cp .env .env.backup
cp -R data data.backup
git status --short
```

不要把 `.env`、`data/mysql`、`data/storage` 提交到 Git。

### 第二步：启动临床系统

```bash
docker compose up -d --build
docker compose ps
curl http://localhost:8000/health
```

### 第三步：确认登录链路

- 打开 `http://localhost:8080/platform/login`。
- 确认静态登录是否只是预览需求。
- 如果需要真实登录，先修复 `PlatformLoginView.vue` 与 `useAuthStore` 的连接。

### 第四步：确认 Dify

- 打开远程 Workflow 页面。
- 确认 Workflow 已发布。
- 确认输入变量名为 `dialog`、`doctor_test`。
- 确认 API Key 在 `.env` 中，不要打印。

### 第五步：跑通真实业务

1. 准备一份支持的音频：`.mp3`、`.wav`、`.flac`、`.opus` 或 `.m4a`。
2. 准备一份第一列为项目、第二列为评分的 HAMD Excel。
3. 创建任务。
4. 观察任务状态从“待处理”到“转录中”再到“质控中”和“已完成”。
5. 打开任务详情，检查 17 个项目。
6. 点击某一行“复核”，输入复核得分和复核意见并保存。
7. 刷新详情页，确认该行显示“已复核”且复核信息保留。
8. 在列表“更多”中测试删除任务。

## 15. 交接结论

项目的主体功能和 Docker 部署骨架已经完成，当前核心业务链路是：

```text
音频 + 医生评分 Excel
    -> 讯飞 ASR
    -> Dify Workflow
    -> 逐项目 AI 质控结果
    -> 人工逐项目复核
```

接手时最需要优先确认的不是目录结构，而是三件事：

1. 当前工作区未提交修改是否全部保留并提交。
2. 平台静态登录是否要改成真实后端登录。
3. 远程 Dify Workflow、API Key 和 HAMD Excel 输入是否仍可用。

其他部署、备份和迁移细节可继续参考：

- [`DOCKER部署.md`](DOCKER部署.md)
- [`README.md`](README.md)
- [`docs/项目交接文档.md`](docs/项目交接文档.md)
- [`dify/README.md`](dify/README.md)
